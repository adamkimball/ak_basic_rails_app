<header class="navbar">
  <a class="navbar_item" href="/">Home</a>
  <% if current_user.present? %>
    <%= button_to 'Log Out', destroy_user_session_path, method: :delete %>
  <% else %>
    <a class="navbar_item" href="/users/sign_in">Sign In</a>
    <a class="navbar_item" href="/users/sign_up">Sign Up</a>
  <% end %>
</header>